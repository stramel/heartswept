---
label: Author
hide_body: true
fields:
- name: name
  type: text
  config:
    required: true
  label: Name
- name: profile_picture
  type: file
  config:
    maxSize: 64
  label: Profile Picture
- name: bio
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Bio
